{% extends 'base.html.twig' %}
{% block title %}{{parent()}}Class Detail{% endblock %}
{% block contenu %}
<div class="card-box">
    <div class="row">
    <h1 class="text-center">Classe Details</h1>

    <table class="table table-striped">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ classe.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ classe.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ classe.description }}</td>
            </tr>
        </tbody>
    </table>
    <table>
        <tr>
            <td>
                <a class="btn btn-success" href="{{ path('classe_index') }}">Back to the list</a>
            </td>
            <td>&nbsp;&nbsp;</td>
            <td>
            <a class="btn btn-default" href="{{ path('classe_edit', { 'id': classe.id }) }}">Edit</a>    
            </td>
        </tr>
    </table>
{#             {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }} #}
    </div>
</div>
{% endblock %}
