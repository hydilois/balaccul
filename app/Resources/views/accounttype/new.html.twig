    {{ form_start(form, {'method': 'POST', 'action': path('accounttype_new')}) }}
    <div class="row">
        <div class="col-md-offset-2 col-md-8">        
        <div class="form-group">
            {{ form_label(form.name, "Type of the  account") }}
            {{ form_widget(form.name, {'attr':{'class':'form-control'}}) }}
        </div>
        <div class="form-group">
            {{ form_label(form.internalAccount, "Internal Account Category") }}
            {{ form_widget(form.internalAccount) }}
        </div>
        <div class="form-group">
            {{ form_label(form.description, "Description of the account") }}
            {{ form_widget(form.description, {'attr':{'class':'form-control'}}) }}
        </div>
        <input class="btn btn-primary" type="submit" value="Save" />
    </div>
    </div>
    {{ form_end(form) }}