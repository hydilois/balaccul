{{ form_start(edit_form, {'method': 'POST', 'action': path('accounttype_edit',{'id':accountType.id} )}) }}
<div class="row">
    <div class="col-md-offset-3 col-md-6">        
    <div class="form-group">
        {{ form_label(edit_form.name, "Name of the Entity Type") }}
        {{ form_widget(edit_form.name, {'attr':{'class':'form-control'}}) }}
    </div>

    <div class="form-group">
        {{ form_label(edit_form.internalAccount, "Internal Account Category") }}
        {{ form_widget(edit_form.internalAccount) }}
    </div>

    <div class="form-group">
    {{ form_label(edit_form.description, "Description of the account type") }}
    {{ form_widget(edit_form.description, {'attr':{'class':'form-control'}}) }}
    </div>
</div>
</div>
<table>
    <tr>
        <td>
            <input class="btn btn-primary" type="submit" value="Save" />
        </td>
{{ form_end(edit_form) }}
        <td>&nbsp;&nbsp;</td>
        <td>{{ form_start(delete_form) }}
            <input class="btn btn-danger" type="submit" value="Supprimer">
            {{ form_end(delete_form) }}
        </td>
    </tr>
</table>

