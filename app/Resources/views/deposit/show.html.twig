{% extends 'base.html.twig' %}

{% block body %}
    <h1>Deposit</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ deposit.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('deposit_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('deposit_edit', { 'id': deposit.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
